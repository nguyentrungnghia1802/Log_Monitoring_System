spring:
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/log_monitor_test}

logging:
  level:
    root: INFO
    com.example.logmonitor: INFO

management:
  endpoint:
    shutdown:
      enabled: true
  endpoints:
    web:
      exposure:
        include: health,shutdown
